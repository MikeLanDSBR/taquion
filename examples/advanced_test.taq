package main

func main() {
    const a = 20;
    const b = 30;
    let resultado = 0; // Mudança de 'const' para 'let'

    if a < b {
        const temp = a + b;
        resultado = temp * 2;
    } else {
        const temp = b - a;
        resultado = temp / 2;
    }

    return resultado;
}