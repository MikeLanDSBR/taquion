package main

func main() {
    const a = 20;
    const b = 30;
    const resultado = 0;

    if a < b {
        const temp = a + b;
        resultado = temp * 2;
    } else {
        const temp = b - a;
        resultado = temp / 2;
    }

    return resultado;
}
